[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "ollama>=0.6.1",
    "pgvector>=0.4.2",
    "psycopg2-binary>=2.9.11",
    "pytest-asyncio>=1.3.0",
    "sqlmodel>=0.0.37",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
